##   一、应用管理
✅ 应用列表（分页、搜索、重要性标签展示）
✅ 应用详情（基本信息、重要性编辑）
✅ 重要性审批策略（轻：无审批；中：仅生产审批；重：TEST/生产审批）

##   二、镜像管理
✅ 镜像列表（分页、按名称/创建人搜索）
✅ 新增/编辑镜像（软删除，已发布环境不可删除）
✅ 镜像发布状态标记（DEV、TEST、PROD）
✅ 发布时自动更新镜像状态

##   三、集群管理（多级）
3.1 物理集群注册
✅ 注册物理 K8s 集群（名称、API Endpoint、kubeconfig 加密存储）
✅ 物理集群列表（支持分页、搜索）
✅ 健康检查定时任务（每30秒探测，连续3次失败标记 offline）
✅ 编辑/删除物理集群（删除前检查依赖）

3.2 环境-命名空间映射
✅ 为物理集群配置环境（dev/test/prod）对应的 K8s Namespace
✅ 映射的增删查

3.3 业务集群（Deployment）
✅ 创建业务集群（选择物理集群、环境，填写名称、实例数、CPU、内存等）
✅ 业务集群列表（展示环境、区域、实例数、状态等）
✅ 编辑业务集群（扩缩容、修改 CPU/内存，异步任务 + 轮询状态）
✅ 删除业务集群（需解绑服务配置且实例数为0）
✅ 服务配置解绑操作
✅ 机器（Pod）列表展示（展开集群行动态加载，支持上下线模拟）

##   四、发布管理
✅ 创建发布任务（选择集群、镜像，填写发布策略）
✅ 发布策略配置（批次比例、批次等待时间、超时时间、预热时间、健康检查路径/端口）
✅ 审批流程（根据应用重要性自动判断是否需要审批，弹窗同意/拒绝）
✅ 发布执行：
检查 Deployment 是否存在，不存在则创建
应用完整规格（副本数、CPU/内存、镜像、滚动更新策略）
设置 maxUnavailable = batchRatio% 实现分批滚动更新
支持超时控制
健康检查（主动 HTTP 探测 Pod 的 health 接口）

✅ 发布状态轮询（前端轮询 /api/releases/{id}/status）
✅ 发布历史记录（按应用查询）
✅ 发布成功后同步 Pod 信息到 pod_instance 表

##   五、服务配置
✅ 服务配置列表（增删改查）
✅ 配置项：名称、类型（ClusterIP/NodePort/LoadBalancer）、端口、目标端口、协议、启用状态

##   六、通知配置
✅ 通知订阅列表（增删改查）
✅ 类型：发布通知、扩缩容通知
✅ 通知方式：邮箱列表（逗号分隔）、Webhook URL

##   七、K8s 集成与基础设施
✅ 多集群客户端管理（KubernetesClientFactory，缓存客户端，支持自动解密 kubeconfig）
✅ Deployment 操作：创建、更新镜像、扩缩容、修改 CPU/内存、删除
✅ 滚动更新策略动态配置（maxUnavailable, maxSurge）
✅ Pod 信息获取与同步
✅ 健康检查（HTTP GET 请求 Pod IP + 端口 + 路径）
✅ 定时任务（集群健康检查）

##   八、前端界面
✅ 侧边栏菜单（应用管理、镜像管理、集群管理、环境映射管理等入口）
✅ 应用详情页（基本信息卡片、Tab 切换：集群/镜像/服务/通知）
✅ 发布浮层（右侧 Drawer，含发布记录、发布操作、策略折叠面板）
✅ 各类 CRUD 弹窗（集群、镜像、服务、通知、环境映射）
✅ 加载状态、错误提示、成功提示
✅ 分页、搜索、表格操作按钮

##   九、后端架构
✅ Spring Boot + MyBatis-Plus
✅ 分页查询、逻辑删除（@TableLogic）
✅ 异步任务（CompletableFuture）用于集群更新、发布执行
✅ 事务管理（@Transactional）
✅ 统一异常处理、Result 封装
✅ JWT 拦截器（部分接口放行）
✅ CORS 配置

##   十、数据库表
已创建并使用的表：

application（应用）
image（镜像）
cluster（业务集群）
registered_cluster（物理集群）
cluster_env_namespace（环境-命名空间映射）
service_config（服务配置）
notification（通知配置）
release_task（发布任务）
approval_record（审批记录）
pod_instance（Pod 实例）
cluster_update_task（集群更新任务）

##   待完善或未实现的功能（根据讨论）
✋ 批次间等待时长（batchWaitTime）在原生 K8s 滚动更新中无法直接实现，若需要严格等待需手动分批（未实现）

✋ 磁盘（disk）字段暂不支持修改（K8s 动态调整存储较复杂）

✋ AI 应用管理、DR 容灾、PaaS 迁移等高级功能（未涉及）


## TODO
可观测性增强：集成监控（Prometheus/ Metrics）、日志聚合（Loki/ELK）、调用链（Jaeger），在发布详情中展示实时指标、错误率等，辅助判断发布是否正常。
自动化回滚：基于健康检查或监控指标（如错误率超过阈值）自动触发回滚，减少人工介入。
流量灰度（金丝雀/蓝绿）：利用 Service Mesh（Istio）或 Nginx ingress 实现基于权重的流量切分，逐步放量，发布更精细。
发布审核工作流增强：支持多级审批、自定义审批人、钉钉/企微通知、审批超时自动处理。
镜像安全扫描：在镜像管理中加入漏洞扫描（集成 Trivy/Clair），展示漏洞报告，阻止高危镜像发布。
多租户与权限：基于 RBAC 控制不同用户/团队对应用、集群的操作权限。
跨集群灾备与迁移：一键将应用从源集群迁移到目标集群（同环境），实现容灾演练。
成本分析：展示各集群、应用、环境的资源使用量和成本（基于云厂商 API 或估算）。
GitOps 集成：将发布任务转化为 Git PR，通过 ArgoCD 或 Flux 同步，实现声明式配置。
AI 辅助分析：根据历史发布成功率、资源使用趋势，预测发布风险或推荐扩缩容阈值。
